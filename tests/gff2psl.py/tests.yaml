
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

withoutseqs:
        stdin: hg19.small.gtf.gz
        outputs: [stdout]
        references: [withoutseqs.psl.gz]
        options: 
