
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

same:
    stdin: null
    outputs: [stdout]
    references: [same.tsv]
    options: --regex-id=".*/(.*.bam)" <DIR>/small.bam <DIR>/small.bam 
