
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

basic_test:
    outputs: [stdout]
    stdin: small_genome.fasta 
    references: [test_basic.tsv]
    options:

cds_test:
    outputs: [stdout]
    stdin: CCDS_nucleotide.short.fna 
    references: [test_cds.tsv.gz]
    options: -c
