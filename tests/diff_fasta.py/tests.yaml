
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version


diff:
    stdin: null
    outputs: [stdout]
    references: [diff.out]
    options: --output-section=missed --output-section=seqdiff <DIR>/a.fasta.gz <DIR>/b.fasta.gz
