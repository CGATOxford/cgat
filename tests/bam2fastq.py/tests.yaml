
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

default_test:
    stdin: null
    outputs: [example.1.fastq.gz,example.2.fastq.gz]
    references: [example.1.fastq.gz,example.2.fastq.gz]
    options: <DIR>/example.bam example.1.fastq.gz example.2.fastq.gz

