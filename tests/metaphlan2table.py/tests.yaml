
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

rel_ab:
    stdin: relab.metaphlan
    outputs: [stdout]
    options: --sequence-type=rel_ab --log=relab.log
    references: [relab.metaphlan.tsv]
    description: output tab format for relative abundance estimations

readmap:
    stdin: readmap.metaphlan
    outputs: [stdout]
    options: --sequence-type=read_map --log=relab.log
    references: [readmap.metaphlan.tsv]
    description: output tab format for metaphlan read map file

        

