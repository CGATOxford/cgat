version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

basic_test:
    stdin: test.fasta.gz
    outputs: [stdout]
    references: [basic_test_reference_content.tsv]
    options: --kmer-size 4  --random-seed=1
