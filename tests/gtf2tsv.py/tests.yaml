
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

tsv:
        stdin: 
        outputs: [stdout]
        references: [out.tsv]
        options: <DIR>/hg19.small.gtf.gz <DIR>/hg19.small.gtf.gz
