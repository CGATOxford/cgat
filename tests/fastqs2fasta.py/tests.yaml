version:
   stdin: null
   outputs: [stdout]
   references: []
   options: --version


basic_test:
    stdin: null
    outputs: [ stdout ]
    references: [basic_test_reference.fasta.gz ]
    options: --first-fastq-file <DIR>/test.fastq.1.gz --second-fastq-file <DIR>/test.fastq.2.gz 
    description: basic test for interleaving two fastq files (read1 and read2) into fasta format
