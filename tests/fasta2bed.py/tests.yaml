
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

gaps:
        stdin: in.fasta
        outputs: [stdout]
        references: [gaps.bed]
        options:
      
