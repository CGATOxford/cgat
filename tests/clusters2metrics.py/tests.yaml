version:
 stdin: null
 outputs: [stdout]
 references: []
 options: --version

metrics:
 stdin: null
 outputs: [stdout]
 references: [metrics.tsv]
 options: --method=metrics --log=metrics.log %DIR%/test-agg-clusters.tsv
