
version:
    stdin: null
    outputs: [stdout]
    references: []
    options: --version

small:
    stdin: small.bam
    outputs: [stdout, overrun]
    references: [small.tsv, overrun]
    options: --gtf-file=<DIR>/hg19.chr19.gtf.gz
