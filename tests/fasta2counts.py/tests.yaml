version:
    stdin: null
    outputs: [stdout]
    references: [version_test.out]
    options: --version

basic:
    stdin: null 
    outputs: [stdout]
    references: [test1.tsv]
    options: --genome-file=%DIR%/small_genome

