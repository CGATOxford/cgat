========================
CpG Coverage
========================

The following plots display the coverage of CpG by reads.

.. report:: Mapping.CpGCoverage
   :render: line-plot
   :as-lines:
   :transform: aggregate
   :logscale: x
   :tf-aggregate: normalized-total,reverse-cumulative

   Cumulative plots of CpG coverage

